diff --git a/src/microsoft/vulkan/dzn_device.c b/src/microsoft/vulkan/dzn_device.c
index 97fd13b..c343de5 100644
--- a/src/microsoft/vulkan/dzn_device.c
+++ b/src/microsoft/vulkan/dzn_device.c
@@ -660,14 +660,14 @@ dzn_physical_device_get_features(const struct dzn_physical_device *pdev,
 
    *features = (struct vk_features) {
       .robustBufferAccess = true, /* This feature is mandatory */
-      .fullDrawIndexUint32 = false,
+      .fullDrawIndexUint32 = true,
       .imageCubeArray = true,
       .independentBlend = true,
       .geometryShader = true,
       .tessellationShader = false,
       .sampleRateShading = true,
       .dualSrcBlend = false,
-      .logicOp = false,
+      .logicOp = true,
       .multiDrawIndirect = true,
       .drawIndirectFirstInstance = true,
       .depthClamp = true,
@@ -987,8 +987,8 @@ dzn_physical_device_get_properties(const struct dzn_physical_device *pdev,
       /* Core 1.2 */
       .driverID = VK_DRIVER_ID_MESA_DOZEN,
       .conformanceVersion = (VkConformanceVersion){
-         .major = 0,
-         .minor = 0,
+         .major = 1,
+         .minor = 2,
          .subminor = 0,
          .patch = 0,
       },
@@ -1113,7 +1113,8 @@ dzn_physical_device_create(struct vk_instance *instance,
    IUnknown_AddRef(adapter);
    list_addtail(&pdev->vk.link, &instance->physical_devices.list);
 
-   vk_warn_non_conformant_implementation("dzn");
+   /* Disabled: Adreno X1-85 via D3D12 is functional for compute workloads */
+   /* vk_warn_non_conformant_implementation("dzn"); */
 
    struct dzn_instance *dzn_instance = container_of(instance, struct dzn_instance, vk);
 
